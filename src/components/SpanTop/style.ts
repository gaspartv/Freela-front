import styled from "styled-components";

export const SpanTop = styled.span`
    position: relative;
    top: 0;
    left: 0;
    display: block;
    width: 100vw;
    height: 1.5rem;
    background-color: var(--Primary);
`;