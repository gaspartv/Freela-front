import styled from "styled-components";

export const SpanTop = styled.span`
  position: fixed;
  top: 0;
  left: 0;
  display: block;
  width: 100vw;
  min-width: 300px;
  height: 1.5rem;
  background-color: var(--Primary);
  z-index: 101;
`;
